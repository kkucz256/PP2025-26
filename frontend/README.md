Uruchomienie frontendu lokalnie przez Docker Compose:

1. docker-compose up --build
2. Otwórz przeglądarkę: http://localhost:3000

Frontend jest prostą stroną statyczną, która zawiera link do dokumentacji API (http://localhost:8000/api/docs).